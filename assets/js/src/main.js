/**
 * UPPA Base — JavaScript bundle entry point.
 *
 * esbuild uses this file as the single entry when building
 * assets/js/dist/main.min.js. Individual source files remain
 * importable separately for child-theme use.
 *
 * @package uppa-base
 */

import './navigation';
import './skip-link-focus';
